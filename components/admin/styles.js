import { styled } from '@mui/material/styles';
import { Box } from '@mui/material';

export const AdminStyles = styled(Box)(() => ({
  marginTop: '20px',
  '.menu': {
    border: '1px solid #F77A0C',
    borderRadius: '6px',
    padding: '16px'
  },
}))