import { styled } from '@mui/material/styles';
import { Box } from '@mui/material';

export const TopStyles = styled(Box)(() => ({
  width: '100%',
  border: '1px solid #F77A0C',
  borderRadius: '6px',
  margin: '20px 0',
  padding: '18px'
}))